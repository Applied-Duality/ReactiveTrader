<Window x:Class="Adaptive.ReactiveTrader.Server.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ReactiveTrader Server" 
        Height="100" 
        Width="525"
        ResizeMode="NoResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Server state:" Margin="4" />
        <TextBlock Grid.Row="0" Grid.Column="1" Margin="4" Name="ServerStatusTextBlock" Text="Stoped" />
        <Button Grid.Row="0" Grid.Column="2" Margin="4" Name="ServerButton" Click="ServerButton_Click">Start</Button>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Throughput" Margin="4"/>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="4" >
            <TextBlock Name="ThroughputTextBlock"  Text="0" />
            <TextBlock Text=" msg/sec"/>
        </StackPanel>
        <Slider Grid.Row="1" Grid.Column="2" Minimum="0" Maximum="5000" Margin="4" ValueChanged="Slider_ValueChanged" />
    </Grid>
</Window>
