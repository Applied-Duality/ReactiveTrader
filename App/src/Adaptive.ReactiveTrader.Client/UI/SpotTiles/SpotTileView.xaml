<UserControl x:Class="Adaptive.ReactiveTrader.Client.UI.SpotTiles.SpotTileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="{Binding Symbol, Mode=OneWay}"/>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="120" />
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Bid.BigFigures, Mode=OneWay}" />
                    <TextBlock Text="{Binding Bid.Pips, Mode=OneWay}" FontSize="32" />
                    <TextBlock Text="{Binding Bid.TenthOfPip, Mode=OneWay}" />
                </StackPanel>
            </Button>

            <TextBlock Text="{Binding Spread, Mode=OneWay}" Grid.Column="1" />

            <Button Grid.Column="2">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Bid.BigFigures, Mode=OneWay}" />
                    <TextBlock Text="{Binding Bid.Pips, Mode=OneWay}" FontSize="32" />
                    <TextBlock Text="{Binding Bid.TenthOfPip, Mode=OneWay}" />
                </StackPanel>
            </Button>
        </Grid>
        
        <TextBox Grid.Row="2" Text="{Binding Notional}" />
    </Grid>
</UserControl>
