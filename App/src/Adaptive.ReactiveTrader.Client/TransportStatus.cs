namespace Adaptive.ReactiveTrader.Client
{
    public enum TransportStatus
    {
        Connecting,
        Connected,
        ConnectionSlow,
        Reconnecting,
        Reconnected,
        Closed
    }
}